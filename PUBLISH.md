# Публикация в Яндекс Играх

Игра «Перекуп BMW» — single-file HTML5 (React + Vite, `vite-plugin-singlefile`).
Все изображения встроены в бандл data-URI, внешних запросов и ссылок нет (п. 8.4.2).

## Сборка

```bash
npm ci
npm run build
node scripts/pack.mjs
```

Изображения встроены в исходник (`src/assets-data.ts`) — отдельный шаг для них не нужен.

Готовый архив: `publish/bmw-clicker-yandex.zip` (в корне архива — `index.html`, п. 1.22;
размер файлов далеко ниже лимита 100 МБ, п. 1.21). Локальная проверка: `npm run dev`.

## Скачать готовую сборку

После отправки изменений в ветку `arena/01a03a99-bmw-clicker` GitHub Actions
автоматически проверяет типы, восстанавливает 33 изображения, собирает игру,
проверяет пакет и обновляет постоянный Release `latest-build`:

https://github.com/MrZorrex/bmw-clicker/releases/download/latest-build/bmw-clicker-yandex.zip

Также ZIP доступен как артефакт последнего запуска workflow **Build Yandex Games release**
на вкладке **Actions**. В Консоль Яндекс Игр загружается ZIP целиком, распаковывать
его перед загрузкой не нужно.

## Что уже сделано в коде (соответствие требованиям)

| Пункт | Статус |
|---|---|
| 1.1 SDK подключён и инициализируется (`YaGames.init()`) | ✅ `index.html` + `src/game/yandex.ts` |
| 1.3 звук останавливается при потере фокуса | ✅ WebAudio suspend на blur/visibilitychange |
| 1.6.1.8 / 1.6.2.7 лонгтап и клик не выделяют поле, нет контекстного меню | ✅ CSS + `contextmenu` prevent |
| 1.9 прогресс фиксируется после значимых действий | ✅ `saveNow()` + автосейв 2.5 с + облако 20 с |
| 1.10.2 нет браузерной прокрутки / swipe-to-refresh | ✅ `overflow: hidden`, `overscroll-behavior` |
| 1.12 монетизация | ✅ interstitial (не чаще 4 мин, паузы) + rewarded |
| 1.13.x инап-покупки | ✅ консумация, каталог из SDK, выдача = описанию |
| 1.19.2 `LoadingAPI.ready()` | ✅ при готовности UI |
| 1.19.3 `GameplayAPI.start/stop` | ✅ меню/модалки/реклама → stop |
| 1.19.4 `game_api_pause/resume` | ✅ звук на паузе |
| 4.5 rewarded по желанию с понятной наградой | ✅ «Реклама → контейнер из Тольятти» |
| 4.7 пауза звука и геймплея на рекламе | ✅ `adActive` + `setSoundSuspended` |

Вне платформы игра работает офлайн: все вызовы SDK деградируют в no-op, прогресс — в localStorage.

## Консоль разработчика — пошагово

### 1. Профиль и договор (quick-start)

1. Создайте профиль в [Консоли](https://games.yandex.ru/console), укажите язык связи.
2. Заключите договор: РФ юрлицо/ИП — Единая лицензионная схема прямо в Консоли;
   иначе — регистрация в [РСЯ](https://partner.yandex.ru/) как самозанятый/ИП + реквизиты.

### 2. Черновик игры

1. **Архив** — загрузите `publish/bmw-clicker-yandex.zip` или ZIP из GitHub Release.
2. **Название** — «Перекуп BMW» (идентично в игре и материалах, п. 5.1.3).
3. **Языки** — только русский (игра одноязычная, п. 2.14 не применяется).
4. **Облачные сохранения** — включите опцию «Игра использует облачные сохранения» (п. 1.11;
   игра хранит прогресс через `player.setData/getData`, ключ `save`).
5. **Категории** — «Казуальные» / «Кликеры» (содержание соответствует жанру, п. 2.3).
6. **Возрастной тег** — 6+ (п. 2.7).
7. **Иконка** 512×512 и **обложка** — без скриншотов из игры (п. 5.6), без рамок/скруглений (п. 8.3.3).
8. **Скриншоты** — реальный геймплей ≥70% кадра (п. 5.1.1.2).
9. **Поддерживаемые платформы** — десктоп + мобильные; **ориентация** — любая.
10. Заполните **Об игре** и **Как играть** (полное описание управления, п. 2.2).

### 3. Монетизация

1. Подключите рекламную монетизацию (РСЯ → реквизиты → оферта акцептована).
2. Полноэкранная и rewarded-реклама вызываются игрой через `ysdk.adv` — настройки в Консоли не требуются.

### 4. Инап-покупки (вкладка «Инап-покупки»)

Код уже готов: покупки обрабатываются на клиенте (`getPayments()` без `signed`),
есть консумация и проверка необработанных покупок при каждом запуске (п. 1.13.1).

Добавьте товары (id строго как в коде — `src/data/products.ts`):

| ID | Тип | Название | Описание (≤200 символов) |
|---|---|---|---|
| `cash_pile` | расходный (консумируется) | Горсть наличных | Мгновенно выдаёт наличные: 35% от цены следующей машины. Можно покупать многократно. |
| `vip_dealer` | постоянный (не консумируется) | Статус «Перекуп года» | Навсегда +25% ко всему доходу: клик, пассив и автокликер. Действует во всех кругах. |

- **Стоимость в янах** — целое число (например, 39 и 99 ян; ориентируйтесь на метрики).
- **Иконка** — PNG 256×256 на каждый товар или дефолтная в «Настройках».
- ⚠️ Список активных покупок в Консоли должен совпадать с игрой (п. 1.13.6):
  каталог в игре строится из SDK — удалили товар в Консоли → он исчезнет из игры автоматически.
  Если покупки не нужны — просто не добавляйте их: кнопка магазина не появится (п. 1.12
  при этом закрыт рекламой).
- **Тест**: добавьте свой логин Яндекс ID в «Список логинов для тестовых покупок»
  (Инап-покупки → Настройки) — тестовые покупки бесплатны, но обязательно
  консумируются игрой, иначе модерация не пройдёт (п. 1.13.1).

### 5. Перед отправкой на модерацию

- Протестируйте черновик (кнопка тестирования в Консоли): загрузка, интро, клики,
  магазин, контейнеры-рулетка, престиж, сворачивание вкладки (звук глушится),
  реклама не чаще 4 минут, покупки тестовым логином.
- В DevTools-консоли не должно быть ошибок (п. 6.4).
- Отправьте на модерацию (3–5 рабочих дней). При отказе — причины придут на почту
  из профиля разработчика.
